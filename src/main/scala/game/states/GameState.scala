package game.states

import java.awt.Graphics2D

class GameState {
//TODO: for now this class is only a skeleton
  
  def init() : Unit = {
    //TODO: Remove ?
  }
  
  def end() : Unit = {
    
  }
  
  def pause() : Unit = {
    
  }
  
  def resume() : Unit = {
    
  }
  
  def render(g : Graphics2D) : Unit = {
    
  }
}